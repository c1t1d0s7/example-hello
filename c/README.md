# Example Hello for C Language

## Build

Build a binary with dynamically linked
```bash
gcc hello.c -o hello
```

Build a binary with statically linked
```bash
gcc hello.c -o hello-static --static
```
